import { DemoFormControlDirective } from './demo-form-control.directive';

describe('DemoFormControlDirective', () => {
  it('should create an instance', () => {
    const directive = new DemoFormControlDirective();
    expect(directive).toBeTruthy();
  });
});

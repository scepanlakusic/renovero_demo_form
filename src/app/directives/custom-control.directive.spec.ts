import { CustomControlDirective } from './custom-control.directive';

describe('CustomControlDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomControlDirective();
    expect(directive).toBeTruthy();
  });
});
